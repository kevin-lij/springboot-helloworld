kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

 steps:
   - name: greeting
     commands:
     - echo hello world
     - ls

#steps:
#  - name: build
#    commands:
#      - docker build -t ra-backend:latest .
#
#  - name: deploy
#    when:
#      branch:
#        - main
#    commands:
#      - cd /root/workspace
#      - docker-compose up -d


trigger:
  branch:
    - master
  event:
    - pull_request
    - push